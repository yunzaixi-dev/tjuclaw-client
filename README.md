# Product Frontend

产品前端目录，与根目录运行中的 Wiki 独立。这里使用 React + shadcn。

首次实现时在本目录建立独立 package.json、src 和测试入口，再接入仓库命令。
框架、路由和构建工具应在实际开发时确定，不为目录骨架安装第二套依赖。
只有出现多个可运行 JS 包后才引入 pnpm workspace；不重复实现后端业务规则。

配置模板只能使用无效示例值。面向用户的资源不包含私人研究资料。
