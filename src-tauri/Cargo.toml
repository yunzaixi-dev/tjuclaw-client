[package]
name = "tjuclaw-client"
version = "0.0.0"
description = "TJUClaw native client"
authors = ["TJUClaw contributors"]
edition = "2021"
publish = false

[lib]
name = "tjuclaw_client_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
