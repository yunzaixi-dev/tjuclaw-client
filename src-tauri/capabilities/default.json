{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main",
  "description": "Only read the app version; no filesystem, shell or network plugins.",
  "windows": ["main"],
  "permissions": ["core:app:allow-version"]
}
